{
  "family": "goorm-popcorn-${ENVIRONMENT}-order-service",
  "networkMode": "awsvpc",
  "requiresCompatibilities": [
    "FARGATE"
  ],
  "cpu": "512",
  "memory": "1024",
  "executionRoleArn": "arn:aws:iam::${AWS_ACCOUNT_ID}:role/goorm-popcorn-${ENVIRONMENT}-ecs-task-execution-role",
  "taskRoleArn": "arn:aws:iam::${AWS_ACCOUNT_ID}:role/goorm-popcorn-${ENVIRONMENT}-ecs-task-role",
  "containerDefinitions": [
    {
      "name": "order-service",
      "image": "${AWS_ACCOUNT_ID}.dkr.ecr.ap-northeast-2.amazonaws.com/goorm-popcorn-order:${IMAGE_TAG}",
      "essential": true,
      "portMappings": [
        {
          "containerPort": 8080,
          "protocol": "tcp",
          "name": "http"
        }
      ],
      "environment": [
        {
          "name": "SPRING_PROFILES_ACTIVE",
          "value": "${ENVIRONMENT}"
        },
        {
          "name": "SERVER_PORT",
          "value": "8080"
        },
        {
          "name": "DB_HOST",
          "value": "${DB_HOST}"
        },
        {
          "name": "DB_PORT",
          "value": "${DB_PORT}"
        },
        {
          "name": "DB_NAME",
          "value": "${DB_NAME}"
        },
        {
          "name": "ORDER_DB_USERNAME",
          "value": "${ORDER_DB_USERNAME}"
        },
        {
          "name": "ORDER_DB_PASSWORD",
          "value": "${ORDER_DB_PASSWORD}"
        },
        {
          "name": "ORDER_FLYWAY_USERNAME",
          "value": "${ORDER_FLYWAY_USERNAME}"
        },
        {
          "name": "ORDER_FLYWAY_PASSWORD",
          "value": "${ORDER_FLYWAY_PASSWORD}"
        },
        {
          "name": "REDIS_HOST",
          "value": "${REDIS_PRIMARY_ENDPOINT}"
        },
        {
          "name": "REDIS_PORT",
          "value": "6379"
        },
        {
          "name": "PAYMENT_SERVICE_URL",
          "value": "http://payment-service.goormpopcorn.local:8080"
        },
        {
          "name": "CHECKINS_SERVICE_URL",
          "value": "http://checkin-service.goormpopcorn.local:8080"
        },
        {
          "name": "STORE_SERVICE_URL",
          "value": "http://store-service.goormpopcorn.local:8080"
        },
        {
          "name": "USER_SERVICE_URL",
          "value": "http://user-service.goormpopcorn.local:8080"
        },
        {
          "name": "GATEWAY_URL",
          "value": "${GATEWAY_URL}"
        },
        {
          "name": "JWT_SECRET",
          "value": "${JWT_SECRET}"
        },
        {
          "name": "PASSPORT_SECRET",
          "value": "${PASSPORT_SECRET}"
        },
        {
          "name": "SPRING_DATA_REDIS_HOST",
          "value": "${REDIS_PRIMARY_ENDPOINT}"
        },
        {
          "name": "SPRING_DATA_REDIS_PORT",
          "value": "6379"
        }
      ],
      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "/aws/ecs/goorm-popcorn-${ENVIRONMENT}/order-service",
          "awslogs-region": "ap-northeast-2",
          "awslogs-stream-prefix": "ecs"
        }
      },
      "healthCheck": {
        "command": [
          "CMD-SHELL",
          "curl -f http://localhost:8080/actuator/health || exit 1"
        ],
        "interval": 30,
        "timeout": 5,
        "retries": 5,
        "startPeriod": 300
      },
      "stopTimeout": 60,
      "startTimeout": 180
    }
  ],
  "tags": [
    {
      "key": "Environment",
      "value": "${ENVIRONMENT}"
    },
    {
      "key": "Service",
      "value": "order-service"
    },
    {
      "key": "Project",
      "value": "goorm-popcorn"
    },
    {
      "key": "ManagedBy",
      "value": "application-team"
    }
  ]
}
